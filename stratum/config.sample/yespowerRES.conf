# Resistance (RES): yespower 1.0 N=4096 r=32 of a 140 byte Zcash style header.
# Bitcoin stratum of its miner (ResistancePlatform/resistance-miner): mining.notify has
# hashFinalSaplingRoot as a 10th parameter, the stratum nonce is the first 32 bits of the
# 256 bit header nonce. The coinbase is the one of the daemon (coinbasetxn) with the
# extranonce in its scriptSig. Difficulty in cpuminer units (difficulty 1 = 65536 hashes).
[TCP]
server = yaamp.com
port = 9650
password = tu8tu5

[SQL]
host = yaampdb
database = yaamp
username = root
password = patofpaq

[STRATUM]
algo = yespowerRES
difficulty = 1
max_ttf = 400000000
renting = 0
