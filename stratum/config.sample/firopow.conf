# Firo (FIRO)
# KawPoW stratum protocol (mining.set_target, 64 bit nonces with a 2 byte prefix,
# header hash + mix hash submits): kawpowminer, T-Rex, NBMiner, TeamRedMiner, SRBMiner...
# The share difficulty is the kawpow pool one (difficulty 1 = target 0x00000000ff00...
# (0xffff for firopow/sccpow), about 2^32 hashes): a 30 MH/s GPU finds a share of
# difficulty 0.1 every ~15 s. Each share is verified from the light cache of the epoch
# (16 MB + 128 KB per epoch, built once in a few seconds; the current and next epoch of each coin are kept).
[TCP]
server = yaamp.com
port = 9504
password = tu8tu5

[SQL]
host = yaampdb
database = yaamp
username = root
password = patofpaq

[STRATUM]
algo = firopow
difficulty = 0.1
diff_min = 0.01
diff_max = 100
nicehash = 0.5
max_ttf = 50000
; the kawpow protocol has no extranonce/reconnect messages, and no rented remote jobs
reconnect = 0
renting = 0
; Firo reduces the epoch after block 1205100: epochs above progpow_max_epoch use
; progpow_terminal_epoch (nMaxPPEpoch/nTerminalPPEpoch). The mainnet values 925/650 are the
; default; regtest uses 2 and 1.
;progpow_max_epoch = 925
;progpow_terminal_epoch = 650
