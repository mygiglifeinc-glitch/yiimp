[TCP]
server = yaamp.com
port = 9303
password = tu8tu5

[SQL]
host = yaampdb
database = yaamp
username = root
password = patofpaq

[STRATUM]
algo = minotaurx
difficulty = 0.0002
max_ttf = 50000


; MinotaurX is selected by the block version on multi-algo chains (LCC, MAZA, AVN, PLSR...):
; getblocktemplate is called with {"powalgo":"minotaurx"} and the job version gets the
; pow type 1 in bits 16-23. Defaults, change only for a coin with another rule:
;powalgo = minotaurx
;version_mask = 0x00ff0000
;version_bits = 0x00010000
