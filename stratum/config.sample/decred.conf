# Decred, BLAKE3 proof of work since block 794368 (DCP-0011), gominer stratum.
# dcrd only provides getwork: the block reward goes to the dcrd --miningaddr,
# which must be the pool wallet address. The coin rpc can be dcrwallet (it
# passes getwork through to dcrd). dcrd has no blocknotify option, run
# bin/blocknotify-dcr connected to dcrd instead.

[TCP]
server = yaamp.com
port = 3252
password = tu8tu5

[SQL]
host = yaampdb
database = yaamp
username = root
password = patofpaq

[STRATUM]
algo = decred
difficulty = 1
max_ttf = 1000000000

